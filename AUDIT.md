# v0.2 원문 및 진행 검사

## 원문 보존 방식

- `docs/미개장수족관_설계문서_v2.txt`를 단일 원문으로 사용합니다.
- 설계 문서에서 게임 본문으로 표시된 인용문(`>`)을 문단 단위로 직접 추출합니다.
- 기존 v0.1의 축약문보다 원문 추출 결과를 우선하도록 덮어씁니다.
- 선택 후 반응도 A/B/C 반응 표식 사이의 원문을 별도 페이지로 연결합니다.
- 대사 수정은 원본 문서와 `src/game/originalText.js`의 구간 표식을 기준으로 관리합니다.

## 확인 완료

- 프로덕션 빌드 성공
- 시작 화면 렌더링
- 입장 버튼 동작
- 첫 문단만 표시되는 것 확인
- `다음` 버튼으로 문단이 순서대로 바뀌는 것 확인
- 프롤로그 마지막 문단까지 선택지가 숨겨지는 것 확인
- 마지막 문단에서만 선택지 2개가 나타나는 것 확인
- 선택 반응이 있으면 반응 페이지 종료 전까지 다음 장면으로 이동하지 않음
- 엔딩도 마지막 문장까지 순서 표시
- 장면 및 엔딩 참조 무결성 확인

## 원문에 시나리오가 없는 항목

아래는 원문 부록에서 미확정으로 명시된 내용이므로 새 대사를 만들지 않았습니다.

1. `FLAG_안내판기입` 숨은 선택지 노출 조건
2. `FLAG_이름부정` 획득 지점과 대사
3. 7장 메시지 A/B의 실제 문면
4. 엔딩 3·12의 전용 진입 장면
5. 엔딩 10의 명확한 진입 장면
6. 루프 트위스트를 직접 설명하는 장면

이 항목들은 원문이 추가되기 전까지 게임 화면에서 TODO 또는 기존 판정 보조 경로로 유지됩니다.

# 설계 문서 v2.0 미확정 항목

아래 내용은 원문 부록에서 미확정으로 명시돼 있어 임의로 보충하지 않았습니다.

1. `FLAG_안내판기입` 숨은 선택지 노출 조건
2. `FLAG_이름부정` 획득 지점
3. 7장 메시지의 실제 문면
4. 엔딩 3·12의 전용 진입 경로
5. 엔딩 10의 명확한 진입 시점
6. 오염도 수치 밸런싱
7. 루프 트위스트를 게임 안에서 명시적으로 밝히는 장면

## v0.1의 처리

- 미확정 선택지는 화면에 비활성화된 `TODO`로 보이게 했습니다.
- 엔딩 데이터와 판정 함수는 구현했지만, 확정 진입 경로가 없는 엔딩은 개발 패널에서 상태를 바꿔 시험할 수 있습니다.
- 8장의 실시간 카운트다운은 규칙과 상승량이 확정되지 않아 정적 연출로 구현했습니다.
- 엔딩 판정 충돌 시 설계 문서 13-1의 순서를 따릅니다.
